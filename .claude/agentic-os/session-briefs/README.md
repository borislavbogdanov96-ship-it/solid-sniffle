# Session Briefs

One file per project, written by the `wrap-up` skill at session end and read at the next
session start (see `agent-behavior.md` → Session Start). Each brief says what the project is,
what the last session did, and exactly what to pick up next.

Empty on a fresh install — briefs accumulate as you work.
